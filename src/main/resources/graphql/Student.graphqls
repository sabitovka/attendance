type Student {
    id: ID!,
    fullname: String,
    email: String,
    phone: String,
    address: String,
    group: Group,
    marks(markDate: String = ""): [Mark]
}

extend type Query {
    students: [Student]!
}